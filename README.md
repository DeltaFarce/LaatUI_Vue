## 安装步骤

```
git clone https://github.com/DeltaFarce/LaatUI_Vue.git      // 把模板下载到本地
cd LaatUI-Vue    // 进入模板目录
npm install         // 安装项目依赖，等待安装完成之后，安装失败可用 cnpm 或 yarn
npm run serve       // 运行项目
http://localhost:8080     // 开启服务器，浏览器访问 
npm run build      // 执行构建命令，生成的dist文件夹放在服务器下即可访问
```

